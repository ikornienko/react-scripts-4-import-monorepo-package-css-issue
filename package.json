{
  "description": "Yarn workspaces monorepo",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "engines": {
    "node": ">=10.22.0"
  },
  "scripts": {
    "start": "cd packages/app; yarn start",
    "build": "cd packages/app; yarn build"
  }
}
